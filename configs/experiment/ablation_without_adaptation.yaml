# Table 4 row 'Frozen encoder, no adaptation': the encoder is frozen and no low-rank adapter is attached.
# Anchor: Table 4 row 13; Sec. 2.3; Methods Sec. 4.6
extends:
  data: data/cohort.yaml
  model: model/default.yaml
  train: train/default.yaml

experiment:
  name: ablation_without_adaptation
  seed: 0
  streams: [ct, radiomics, clinical, endoscopy, pathology]
  ablations: {adaptation: true}
  evaluation:
    output_dir: runs
    achievable_rule: staging
    bootstrap_resamples: 2000
    fusion_strategy: cross_attention
    post_hoc_projection: false
    synthetic:
      site_dispersion: 0.30
      feature_noise: 0.55
      endoscopy_hint_weight: 0.35
