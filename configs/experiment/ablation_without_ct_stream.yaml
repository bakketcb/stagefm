# Table 4 row 'Without CT stream': the image tokens are removed and only the non-imaging streams reach the fusion block.
# Anchor: Table 4 row 8; Sec. 2.3
extends:
  data: data/cohort.yaml
  model: model/default.yaml
  train: train/default.yaml

experiment:
  name: ablation_without_ct_stream
  seed: 0
  streams: [ct, radiomics, clinical, endoscopy, pathology]
  ablations: {ct: true}
  evaluation:
    output_dir: runs
    achievable_rule: staging
    bootstrap_resamples: 2000
    fusion_strategy: cross_attention
    post_hoc_projection: false
    synthetic:
      site_dispersion: 0.30
      feature_noise: 0.55
      endoscopy_hint_weight: 0.35
