# Table 4 row 'Without I1 and I2': the reference configuration of the interaction decomposition.
# Anchor: Table 4 row 4; Sec. 2.3
extends:
  data: data/cohort.yaml
  model: model/default.yaml
  train: train/default.yaml

experiment:
  name: ablation_without_both_structural
  seed: 0
  streams: [ct, radiomics, clinical, endoscopy, pathology]
  ablations: {stage_consistency: true
      risk_control: true}
  evaluation:
    output_dir: runs
    achievable_rule: staging
    bootstrap_resamples: 2000
    fusion_strategy: cross_attention
    post_hoc_projection: false
    synthetic:
      site_dispersion: 0.30
      feature_noise: 0.55
      endoscopy_hint_weight: 0.35
